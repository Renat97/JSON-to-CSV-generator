console.log('hello');

var $file = $('#say').val();

$('form').on('submit', (e) => {
  /*
  e.preventDefault();
  $.ajax({
    url: "http://localhost:3000/upload_json",
    method: 'POST',
    data: JSON.parse(file)
  })
  */
});